#include <bits/stdc++.h>
using namespace std;

int main()
{
    // // Type 1
    // vector<int> v;

    // // Type 2 - vector<type>v(N);
    // vector<int> v(5);
    // for (int i = 0; i < v.size();i++)
    // {
    //     cout << v[i]<<" ";
    // }

    // // Type 3 - vector<type>v(N, V);
    // vector<int> v(5, 2);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // // Type 4 - vector<type>v(V2);
    // vector<int> v1(6, 20);
    // vector<int> v(v1);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // // Type 5 - vector<type>v(A, A+N);
    // int n;
    // cin >> n;
    // int a[n];
    // for (int i = 0; i < n; i++)
    // {
    //     cin >> a[i];
    // }
    // vector<int> v(a, a + n);
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }

    // // Type 6 - vector initialize directly
    // vector<int> v = {1, 4, 6, 2, 7};
    // for (int i = 0; i < v.size(); i++)
    // {
    //     cout << v[i] << " ";
    // }
    return 0;
}
